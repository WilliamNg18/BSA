# Task 36 desktop verification

**Status: final integrated acceptance pending.** This index is not a passing
release report. The coordinator must confirm the final runtime deployment
before hosted capture. The approved scope has four playable cases and eleven
steps, with 1280/1440 px functional coverage and new PNGs at 1440 px only.

## Current verification contracts

Discovery confirms **55 live tests in four files**, not 55 executed passes:
eight core checks, two system-design widths, three header perspectives,
22 step/mode audits, four Back/Next walks and sixteen four-case cycles.
The 1440 px On walk also executes the real operator-to-pharmacy hand-off:
completed B is not substituted for the subsequently referred mismatch item.

The separate four-case exact-state matrix retains clocks, IDs, all revisions,
events, verification, both draft maps and capture metadata. Every side visit
must preserve the item, mode and step. The final strengthened matrix also
checks hidden-side temporary Both and explicit origin restoration at every
state, with a snapshot after each button.

Reports fail closed for missing names, duplicate names, skipped checks,
retries, missing or dirty identities, mismatched commits and malformed
evidence. Local HTTP rehearsal and instrumented builds are not hosted
acceptance. Images remain `visualReview: pending` until actually inspected;
axe alone is not manual WCAG certification.

## Development evidence, not release proof

The following development outputs are retained in this session's artifact
storage, not claimed as portable hosted evidence:

| Run | Actual result | Qualification |
| --- | --- | --- |
| Step 0 navigation reproduction | 1 passed, 1 failed | Agent help tooltip blocked Next; original failure retained |
| Same two checks after D's header repair | 2 passed | No forced click, timeout increase or pointer-event bypass |
| First B On / D On state probe | 2 tests passed, 4 actual Both/switched cycles | Local development only |
| First full four-case state run | 7 passed, 1 failed | Off D incorrectly expected an On-only declaration field |
| Second full state run | 7 passed, 1 failed | Manual correction still lacked product/quantity; a separate real Off-capture attestation restriction was repaired by G |
| Corrected coherent state run | 8 passed, 16 actual cycles, no retries, 2.6 minutes | Instrumented build `e799b3774a73bd22d4c8bb69265db771ef3c115d`, `dirty: true`; not clean source-pinned or hosted proof |

The last row predates the additional every-state temporary-perspective return
assertions. Those strengthened checks require a fresh clean run. It does not
inherit a pass from the earlier matrix.

The intermediate baseline test split `e799b37` also misplaced a new test
inside an existing parameterised test. `a208ffe` restores suite-level
registration without dropping the five existing variants. A combined
worktree's earlier unit result does not certify that faulty intermediate
commit. Owners subsequently ran the entire corrected file, not a filtered
new-test-only probe.

## Explicit scope replacements

Retirement follows the owner's four-case instruction, not a reclassification
of functional failures as informational:

| Superseded coverage | Current replacement |
| --- | --- |
| `tests/live/continuous-cycle.spec.ts`, old D-to-payment wording | `four-case-cycle.spec.ts`: the actual D cycle through human release, plus A/B/mismatch, each mode and perspective sequence |
| `tests/live/generic-correction.spec.ts`, extra playable generic item | Actual wrong-pack EPS referral/correction/resubmission on `SYN-FQ123-MISMATCH` |
| `tests/live/perspective.spec.ts`, older multi-item round trip | Sixteen current-case live cycles and the separate full-domain matrix |
| `tests/live/task30.spec.ts`, eight/ten seeds and six-chapter navigation | Four operational items, C/F noninteractive background, eleven named step audits and full Back/Next walks |
| Playable C confirmation / F decision / E operational routes | B's real information-request flow, noninteractive C/F background and negative deep links, A Today no-model automation; six canonical semantics remain pure domain regressions |
| Readable-paper and seeded recheck operational examples | Unreadable D's actual manual/declaration path and real B/D/mismatch correction cycles; no hidden extra store fixtures |

Remaining ordinary browser migrations are phase-specific: G uses its
baseline controls, followed by the real O/P controls and D's mounted strip.
A future-phase selector is not a valid repair for an earlier runtime.
The complete legacy G run executed 762 ordinary tests: 669 passed and
93 failed. The earlier Step 0 inventory of 833 was not a full passing run.

## Historical archives

[Task 30 evidence](../task30/README.md) remains historical. Its interrupted
independent image review is unverified. The new scope does not relabel it.

Step 0 explicitly withdrew 222 inspected 360 px PNGs from the active checkout.
Its verification found 493 retained desktop images and 798 non-image
resources unchanged. There is no standalone per-file removal/hash manifest
to link: the original tool outputs establish those checks. Do not invent an
artifact path or rehash historical manifests as a new acceptance result.
Original Git history and protected references remain unchanged.
