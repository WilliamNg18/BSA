No keys or tokens belong in source code, files or the static bundle.
Secrets live in GitHub repository secrets or Azure.
Any future model call uses a server-side path that holds the key, never the browser.
The repository returns to private when the demo is done.
Reference documents are public by the owner's decision.
